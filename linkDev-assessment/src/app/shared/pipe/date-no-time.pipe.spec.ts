import { DateNoTimePipe } from './date-no-time.pipe';

describe('DateNoTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DateNoTimePipe();
    expect(pipe).toBeTruthy();
  });
});
